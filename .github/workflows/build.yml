name: Build OctoPad VST3

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # ── Windows ──────────────────────────────────────────────────
  build-windows:
    name: Windows VST3
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Upload VST3 (Windows)
        uses: actions/upload-artifact@v4
        with:
          name: OctoPad-Windows-VST3
          path: build/OctoPad_artefacts/Release/VST3/
          if-no-files-found: error

      - name: Upload Standalone (Windows)
        uses: actions/upload-artifact@v4
        with:
          name: OctoPad-Windows-Standalone
          path: build/OctoPad_artefacts/Release/Standalone/
          if-no-files-found: warn

  # ── Linux ────────────────────────────────────────────────────
  build-linux:
    name: Linux VST3
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install JUCE dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev \
            libx11-dev libxext-dev libxrandr-dev \
            libxinerama-dev libxcursor-dev libxi-dev \
            libxcomposite-dev libfreetype-dev \
            libfontconfig1-dev libgl1-mesa-dev \
            ninja-build

      - name: Configure CMake
        run: cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --parallel

      - name: Upload VST3 (Linux)
        uses: actions/upload-artifact@v4
        with:
          name: OctoPad-Linux-VST3
          path: build/OctoPad_artefacts/Release/VST3/
          if-no-files-found: error
